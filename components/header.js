import { Navigation } from './Navigation';

const Header = () => {
  return (
    <header className="flex h-12 items-center justify-end shadow-lg bg-white">
      <Navigation />
    </header>
  );
  }

export default Header;